{
  "name": "@geas/cli",
  "version": "0.1.0",
  "description": "CLI for Geas plugin — wraps all .geas/ file I/O into atomic commands",
  "main": "index.js",
  "scripts": {
    "build": "npx tsc && node dist/check-field-policy.js",
    "bundle": "node esbuild.config.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --test dist/test/schema-template.test.js dist/test/envelope.test.js dist/test/validation-hints.test.js dist/test/input.test.js dist/test/dry-run.test.js"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "esbuild": "^0.28.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "private": true
}
