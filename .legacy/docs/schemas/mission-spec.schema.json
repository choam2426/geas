{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Geas Mission Spec",
  "description": "Canonical mission baseline approved during specifying phase. Captures mission intent, scope, completion criteria, constraints, and user approval.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "mode",
    "name",
    "description",
    "definition_of_done",
    "user_approved",
    "scope",
    "acceptance_criteria",
    "constraints",
    "affected_surfaces",
    "risks",
    "created_at",
    "updated_at"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Stable mission identifier within the Geas runtime.",
      "pattern": "^mission-[0-9]{8}-[a-zA-Z0-9]{8}$"
    },
    "mode": {
      "type": "string",
      "description": "Default operating depth for the mission.",
      "enum": [
        "lightweight",
        "standard",
        "full_depth"
      ]
    },
    "name": {
      "type": "string",
      "description": "Short human-readable mission name.",
      "minLength": 1
    },
    "description": {
      "type": "string",
      "description": "What the mission is meant to do and why it is worth doing. Captures both the user request and the motivation or problem context behind it.",
      "minLength": 1
    },
    "definition_of_done": {
      "type": "string",
      "description": "High-level completion statement used to judge whether the mission is done.",
      "minLength": 1
    },
    "user_approved": {
      "type": "boolean",
      "description": "Whether the user has approved this mission spec as the current basis for execution."
    },
    "scope": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "in",
        "out"
      ],
      "properties": {
        "in": {
          "type": "array",
          "description": "In-scope surfaces or responsibilities owned by this mission.",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "minItems": 1
        },
        "out": {
          "type": "array",
          "description": "Explicitly out-of-scope surfaces or responsibilities for this mission.",
          "items": {
            "type": "string",
            "minLength": 1
          }
        }
      }
    },
    "acceptance_criteria": {
      "type": "array",
      "description": "Concrete criteria derived from definition_of_done and used for phase gates and task derivation.",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "minItems": 1
    },
    "constraints": {
      "type": "array",
      "description": "Non-negotiable constraints that shape how the mission may be executed.",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "affected_surfaces": {
      "type": "array",
      "description": "Surfaces likely to be affected by the mission outcome.",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "risks": {
      "type": "array",
      "description": "Mission-level risks already visible before task execution begins.",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "created_at": {
      "type": "string",
      "format": "date-time"
    },
    "updated_at": {
      "type": "string",
      "format": "date-time"
    }
  }
}
