# Technical Writer Memory

- For CLI docs sweeps, prefer <<<'single-line' for simple JSON, <<'EOF' heredoc (quoted) for multiline, < path for file-backed; precompute per-file grep hit counts during impl contract
