# Orchestration_authority Memory

- record.json gate_result: tier_results need status field, rubric_scores need dimension and passed (not name/score). closure review status: approved/changes_requested/blocked. debt_candidates must be strings not objects. Use .geas/ dir for temp files on Windows.
- For identifier renames, grep the ENTIRE repo not just scoped surfaces. Check docs/protocol/examples/, docs/ko/, and CLAUDE.md. Example files must validate against updated schemas.
