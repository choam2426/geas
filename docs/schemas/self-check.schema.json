{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "self-check.schema.json",
  "title": "GEAS Task Self-Check",
  "description": "Canonical worker self-check recorded after implementation and before independent review.",
  "type": "object",
  "properties": {
    "completed_work": {
      "type": "string",
      "minLength": 1,
      "description": "What the worker believes was actually completed in this task."
    },
    "reviewer_focus": {
      "type": "array",
      "description": "Areas reviewers should inspect first based on the worker's own implementation experience.",
      "items": {
        "type": "string"
      }
    },
    "known_risks": {
      "type": "array",
      "description": "Known risks, weak spots, or unresolved concerns that remain after implementation.",
      "items": {
        "type": "string"
      }
    }
  },
  "required": [
    "completed_work",
    "reviewer_focus",
    "known_risks"
  ],
  "additionalProperties": true
}
