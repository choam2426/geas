{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Geas Mission State",
  "description": "Mission-scoped runtime index. Points to which phase the mission is in and which tasks are currently executing within it.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "mission_id",
    "phase",
    "active_tasks",
    "created_at",
    "updated_at"
  ],
  "properties": {
    "mission_id": {
      "type": "string",
      "pattern": "^mission-[0-9]{8}-[a-zA-Z0-9]{8}$",
      "description": "Mission ID matching the enclosing mission directory."
    },
    "phase": {
      "type": "string",
      "enum": [
        "specifying",
        "building",
        "polishing",
        "consolidating",
        "complete"
      ],
      "description": "Current phase within the mission."
    },
    "active_tasks": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Task IDs currently executing. A single-element array denotes sequential execution; multiple elements denote a parallel batch; an empty array denotes no task in flight."
    },
    "created_at": {
      "type": "string",
      "format": "date-time"
    },
    "updated_at": {
      "type": "string",
      "format": "date-time"
    }
  }
}
